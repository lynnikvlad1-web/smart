services:
  - type: web
    name: my-nodejs-app
    env: node
    buildCommand: "npm install && npm run build"
    startCommand: "node server.js"
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: true
    pullRequestPreviews: true